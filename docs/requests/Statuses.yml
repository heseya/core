StatusReorderRequest:
  content:
    application/json:
      schema:
        properties:
          statuses:
            type: array
            items:
              type: string
              format: uuid
        type: object
