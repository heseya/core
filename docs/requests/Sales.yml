SaleStore:
  content:
    application/json:
      schema:
        required:
          - name
          - priority
          - target_type
          - target_is_allow_list
          - active
        allOf:
          - $ref: './../schemas/Sales.yml#/SaleStore'
        type: object

SaleUpdate:
  content:
    application/json:
      schema:
        required:
          - name
          - priority
          - target_type
          - target_is_allow_list
          - active
        allOf:
          - $ref: './../schemas/Sales.yml#/SaleUpdate'
        type: object
