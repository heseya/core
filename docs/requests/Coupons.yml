CouponStore:
  content:
    application/json:
      schema:
        required:
          - code
          - translations
          - priority
          - target_type
          - target_is_allow_list
        allOf:
          - $ref: './../schemas/Coupons.yml#/CouponStore'
        type: object

CouponUpdate:
  content:
    application/json:
      schema:
        required:
          - code
          - priority
          - target_type
          - target_is_allow_list
        allOf:
          - $ref: './../schemas/Coupons.yml#/CouponUpdate'
        type: object
