CouponStore:
  content:
    application/json:
      schema:
        required:
          - code
          - name
          - type
          - value
          - priority
          - target_type
          - target_is_allow_list
        allOf:
          - $ref: './../schemas/Coupons.yml#/CouponStore'
        type: object

CouponUpdate:
  content:
    application/json:
      schema:
        required:
        allOf:
          - $ref: './../schemas/Coupons.yml#/CouponStore'
        type: object
